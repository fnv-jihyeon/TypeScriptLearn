import {
  create,
  NButton,
  NInput,
  NCard,
  NMessageProvider,
  NNotificationProvider,
  NDialogProvider,
} from 'naive-ui';

export default create({
  components: [
    NButton,
    NInput,
    NCard,
    NMessageProvider,
    NNotificationProvider,
    NDialogProvider,
  ],
});